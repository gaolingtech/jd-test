```sql
-- 用户表
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL UNIQUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    status VARCHAR(20) DEFAULT 'active' CHECK (status IN ('active', 'inactive', 'banned'))
);

-- 商品表
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    category VARCHAR(50),
    price DECIMAL(10,2) NOT NULL CHECK (price >= 0),
    stock INT DEFAULT 0,
    metadata JSONB,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 订单表
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    user_id INT REFERENCES users(id),
    order_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    total_amount DECIMAL(12,2) DEFAULT 0,
    status VARCHAR(20) DEFAULT 'pending' CHECK (status IN ('pending', 'paid', 'shipped', 'completed', 'cancelled'))
);

-- 订单明细表
CREATE TABLE order_items (
    id SERIAL PRIMARY KEY,
    order_id INT REFERENCES orders(id) ON DELETE CASCADE,
    product_id INT REFERENCES products(id),
    quantity INT NOT NULL CHECK (quantity > 0),
    unit_price DECIMAL(10,2) NOT NULL
);
```

## 一、SQL 题（可使用原生 SQL 或 SQLAlchemy / Django ORM 表达）

**题1：查询每个用户的： 用户名 \ 订单总数 \ 订单总金额（所有订单的 total_amount 之和）\ 最近一次订单日期 \ 只显示有订单的用户。**

**题2：以下查询较慢，请分析原因并设计合理的索引。**

```sql
SELECT * FROM orders
WHERE user_id = 12345
AND status = 'paid'
AND order_date >= '2025-01-01'
ORDER BY order_date DESC
LIMIT 50;
```

**题3：请写出以下查询：**

- 提取 `metadata` 中的 `color` 字段
- 查找 `metadata` 包含 `{"brand": "Apple"}` 的行
- 更新 `metadata` 中的某个字段

## 二、Python 编程题

**题4：请使用 `asyncio` + `aiohttp` 并发抓取一个 URL 列表（如 1000 个）的内容，要求：**

- 最大并发数可配置（如 50）
- 单个请求失败不影响整体
- 统计成功/失败数量及总耗时

**题5：请使用 FastAPI（或 Django/Flask）设计一个订单创建接口 `POST /orders`，需满足：**

- 请求参数校验（Pydantic 或 DRF Serializer）
- 数据库事务保证订单主表与明细表同时写入成功
- 使用 Redis 实现基于 `Idempotency-Key` 请求头的幂等性控制
- 扣减商品库存时避免超卖

**题6：多线程 vs 多进程 —— 给定如下两个任务，请分别选择最合适的并发模型并实现：**

1. **任务 A（IO 密集）**：从一个包含 200 个本地图片路径的列表中，逐张读取并计算 MD5，写回数据库。
2. **任务 B（CPU 密集）**：对同一批 200 张图片做缩放 + 加水印（PIL/Pillow），输出到目标目录。

**题7：剧本拆解 —— 请使用 Python 调用 LLM（OpenAI / Claude / 国产模型任选）实现一个函数 `split_script_to_shots(novel_text: str) -> list[Shot]`，将一段小说文本拆解为分镜列表。每个 `Shot` 至少包含：**

- `shot_id` 镜号
- `scene` 场景描述（环境、时间、氛围）
- `characters` 出场角色列表
- `dialogue` 角色台词
- `camera` 运镜建议（特写/中景/远景/推/拉/摇/移）
- `image_prompt` 文生图提示词（英文，含画风、光影、构图）
- `duration` 预估时长（秒）

要求：使用结构化输出（JSON Schema / Pydantic），并对模型返回结果做校验与重试。






